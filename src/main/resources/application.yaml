server:
  port: 8080
  error:
    include-message: always
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/qaJavaBot
    username: root
    password: rootadmin
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
  liquibase:
    change-log: classpath*:db/changelog/changelog-master.yaml